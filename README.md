# 📝 Vibe Task List: Менеджер задач на SvelteKit и IndexDB

## 🚀 Описание проекта
Это приложение-менеджер задач, разработанное для демонстрации навыков **быстрого прототипирования** и использования современного веб-стека. Приложение полностью работает в браузере (не требует бэкенда), используя локальную базу данных **IndexDB** через библиотеку **Dexie.js**.

---

## 🛠️ Стек технологий
* **Фреймворк:** **SvelteKit** (v2+)
* **Язык:** **TypeScript** (Полная типизация всех интерфейсов, Store и компонентов)
* **Стилизация:** **Tailwind CSS 3.x** (с плагином `@tailwindcss/forms` для стилизации полей)
* **База данных:** **IndexDB** через **Dexie.js** (для локального хранения данных)
* **Менеджер пакетов:** `pnpm`

### 🎨 Обоснование выбора UI-библиотеки

Был выбран **Tailwind CSS** вместо готовой UI-библиотеки.

* **Преимущества:** Обеспечивает максимальный **контроль над дизайном** и адаптивностью, что позволило реализовать точный адаптивный лэйаут (`lg:grid-cols-3` и вертикальный стек на мобильных устройствах). Это также лучше демонстрирует навыки работы с CSS и быстрого прототипирования стилей (Vibe Coding).
* **Результат:** Чистый, современный и полностью адаптивный интерфейс.

---

## ✨ Ключевая функциональность (CRUD и Бонусы)
Реализована полная функциональность для управления задачами, включая все обязательные и большинство бонусных требований ТЗ.

* **Полный CRUD (Create, Read, Update, Delete):**
    * Создание новой задачи с валидацией поля Title.
    * Чтение, отображение и **редактирование** существующих записей через модальное окно.
    * Переключение статуса задачи (**Активная/Завершена**).
    * Удаление задачи из списка и базы данных.
* **Поиск и Сортировка:**
    * Быстрый поиск по названию задачи с использованием **debounce** (антидребезг) для оптимизации.
    * Сортировка списка по **дате создания** или по **статусу** (Активные задачи идут первыми).
* **Реактивность:** Использование Svelte Stores для синхронизации состояния между компонентами и Dexie.
* **Пользовательский опыт:**
    * **Адаптивный дизайн** (`< 768px` и `> 768px`) для десктопа и мобильных устройств.
    * **Простая анимация** (`svelte:transition fade`) при добавлении/удалении карточек.

---

## 🏁 Начало работы

### 1. Клонирование и установка
Предполагается, что у вас установлены Node.js и pnpm.

```bash
# Перейдите в папку проекта
cd vibe-coding-task-list

# Установите зависимости проекта (включая Dexie и Tailwind)
pnpm install

# Запуск приложения
pnpm run dev
```

Проект будет доступен по адресу: http://localhost:5173/
